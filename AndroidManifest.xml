<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.talsoft.organizeme"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="14" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="Organize me"
        android:theme="@style/AppTheme" >
        <!-- activity
            android:name="com.talsoft.organizeme.activities.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-->
         <activity android:name="com.talsoft.organizeme.activities.LogInActivity">
              <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
         </activity>
         <activity android:name=".activities.HomeActivity"></activity>
         <activity android:name=".activities.TaskListActivity"></activity>
         <activity android:name=".activities.CreateTaskActivity"></activity>
         <activity android:name=".activities.TagListActivity"></activity>
         <activity android:name=".activities.CreateTagActivity"></activity>
    </application>

</manifest>
